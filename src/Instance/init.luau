local apply = require("@self/apply")
local create = require("@self/create")
local defaults = require("@self/defaults")

local Instance = {
	apply = apply,
	create = create,
	defaults = defaults,
}

return Instance
